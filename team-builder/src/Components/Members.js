import React from "react";

const Members = props => {
return (
<div>
    hello
    {/* {props.MembersForm.map((member, index) => {
    return (
        <div className="member" key={index}>
        <h2>{member.title}</h2>
        </div>
    );
    })} */}
</div>
);
};
export default Members;